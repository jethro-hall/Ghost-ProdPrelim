{
  "name": "02_SUB_POS_RETAIL",
  "nodes": [
    {
      "parameters": {
        "inputSource": "passthrough"
      },
      "id": "a02trigger-pos-retail",
      "name": "When Executed by Core",
      "type": "n8n-nodes-base.executeWorkflowTrigger",
      "typeVersion": 1.1,
      "position": [
        -240,
        256
      ]
    },
    {
      "parameters": {
        "jsCode": "const fs = require('fs');\nconst input = { ...$input.first().json };\n\nif (input.odoo_api_key_or_password) {\n  input.odoo_api_key_or_password = String(input.odoo_api_key_or_password).trim();\n}\n\nconst inputPath = '/tmp/odoo_02_pos_retail_input.json';\nfs.writeFileSync(inputPath, JSON.stringify(input), 'utf8');\n\nreturn [{\n  json: {\n    ok: true,\n    input_path: inputPath,\n    snapshot_id: input.snapshot_id,\n    message: 'Input JSON written for POS retail exporter',\n  },\n}];\n"
      },
      "id": "a02write-input-json",
      "name": "Write exporter input JSON",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -16,
        256
      ]
    },
    {
      "parameters": {
        "command": "node /home/node/.n8n/scripts/02_pos_retail_exporter.js"
      },
      "id": "a02run-pos-exporter",
      "name": "Run POS Retail Exporter",
      "type": "n8n-nodes-base.executeCommand",
      "typeVersion": 1,
      "position": [
        208,
        256
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first().json;\n\nif (item.exitCode !== undefined && Number(item.exitCode) !== 0) {\n  throw new Error(\n    `POS retail exporter failed (exit ${item.exitCode}). stderr: ${item.stderr || ''}`,\n  );\n}\n\nconst stdout = String(item.stdout || '').trim();\nif (!stdout) {\n  throw new Error(\n    `POS retail exporter returned empty stdout. stderr: ${item.stderr || ''}`,\n  );\n}\n\nconst lastLine = stdout.split(/\\r?\\n/).filter(Boolean).pop();\n\ntry {\n  return [{ json: JSON.parse(lastLine) }];\n} catch (e) {\n  throw new Error(\n    `Could not parse POS exporter stdout as JSON: ${e.message}. stdout starts: ${stdout.slice(0, 500)}`,\n  );\n}\n"
      },
      "id": "a02parse-result",
      "name": "Parse Exporter Result",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        432,
        256
      ]
    }
  ],
  "connections": {
    "When Executed by Core": {
      "main": [
        [
          {
            "node": "Write exporter input JSON",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Write exporter input JSON": {
      "main": [
        [
          {
            "node": "Run POS Retail Exporter",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Run POS Retail Exporter": {
      "main": [
        [
          {
            "node": "Parse Exporter Result",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  },
  "meta": {
    "instanceId": "8a47d164-f11f-4733-bd58-cdf1c672ab88"
  }
}