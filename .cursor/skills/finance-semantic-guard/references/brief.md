This skill exists to stop finance semantic drift.

Primary failure mode:
- finance metric request downgraded into ledger search
- keyword matching used as pseudo-classification
- composer receives row dump instead of metric pack

Primary fix:
- metric-first planning
- config-backed account classification
- deterministic metric assembly
- optional supporting ledger evidence only after metric pack exists
